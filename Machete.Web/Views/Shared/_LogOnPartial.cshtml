@using Machete.Web.Helpers

@if (User.Identity.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()
        <!-- Greeting announcing signed in user -->
        <text>@ValidationStrings.Welcome <b>@User.Identity.Name</b>
        [ @if(Html.IsInRole(Html.Role_AM())) 
            { 
                @* Settings console *@
                @Html.ActionLink(ValidationStrings.Settings, "Index", "Account")
            }
            else 
            { 
                @* Settings console *@
                if(!Html.IsInRole(Html.Role_H())) 
                {
                    @* User account console *@
                    @Html.ActionLink("Manage", "Manage", "Account") 
                }
            } ]

@* thought to be entirely unnecessary, but commenting out for now *@
@*         [ @if(Html.IsInRole(Html.Role_H()))  *@
@*             { *@
@*                 @Html.ActionLink(ValidationStrings.LogOff, "LogOff", "HirerAccount") *@
@*             } *@
@*             else *@
@*             { *@
                @Html.ActionLink(ValidationStrings.LogOff, "LogOff", "Account")
@*             } ] *@
        </text>
    }

}
